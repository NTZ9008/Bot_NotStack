# Solution: ระบบแปลง Level เป็น Token สำหรับใช้งานบนเว็บไซต์

เอกสารนี้รวบรวมแนวทางและสถาปัตยกรรม (Architecture) ในการนำระบบเลเวลจากบอท Discord ไปต่อยอดเป็น **Token/เหรียญ** สำหรับใช้แลกเปลี่ยนของรางวัลบนเว็บไซต์ในอนาคต

---

## 🎯 ภาพรวมการทำงาน (Concept)

1. **การ Login บนเว็บ:** ผู้ใช้จะต้องเข้าสู่ระบบเว็บไซต์ด้วยระบบ **Discord OAuth2** (Login with Discord) เพื่อให้เว็บไซต์สามารถระบุตัวตนและจับคู่กับ Discord ID ของผู้ใช้ได้
2. **การดึงข้อมูลและเชื่อมโยง:** เว็บไซต์จะใช้ Discord ID นั้นไปตรวจสอบ Level และ Token (หรือ Coin) ที่ผู้ใช้มีในระบบ
3. **การแปลงและใช้งาน Token:**
   * อาจทำระบบ **"แปลงเลเวลเป็น Token"** แบบกดเองผ่านเว็บไซต์ (เช่น กดปุ่มเพื่อใช้ Level 10 แลกรับ 100 Token)
   * หรือทำระบบ **"รับ Token อัตโนมัติ"** เมื่อผู้ใช้อัพเลเวลจากใน Discord (เช่น ได้ 10 Token ทุกๆ 1 เลเวล)
4. **การแลกของ:** เมื่อผู้ใช้ใช้ Token แลกของบนเว็บไซต์ ระบบจะทำการหัก Token ออกจากฐานข้อมูลส่วนกลาง

---

## 🛠 Solutions สำหรับการเชื่อมต่อข้อมูล

เพื่อให้ Bot (Node.js) และ Website (Backend) สามารถแชร์และอัปเดตข้อมูล Level/Token ร่วมกันได้ นี่คือ 3 แนวทางหลักเรียงตามความเหมาะสม:

### Solution 1: ย้ายระบบไปใช้ Database (⭐️ แนะนำที่สุด)
ระบบในปัจจุบันเก็บข้อมูลไว้ในไฟล์ `levels.json` ซึ่งไม่เหมาะสำหรับการอ่าน/เขียนพร้อมกันจากหลายระบบ (Concurrency) การเปลี่ยนไปใช้ฐานข้อมูลจริงจะแก้ปัญหานี้ได้

* **โครงสร้างการทำงาน:** ทั้ง Discord Bot และ Website จะเชื่อมต่อไปยังฐานข้อมูลเดียวกัน 
* **เครื่องมือที่แนะนำ:** **MongoDB** (ใช้คู่กับ Mongoose), **MySQL** หรือ **PostgreSQL**
* **โครงสร้างข้อมูลเบื้องต้น (Schema):**
  ```json
  {
    "discordId": "797369482657923085",
    "xp": 54,
    "level": 0,
    "tokens": 150  // Field ใหม่สำหรับเก็บเหรียญ
  }
  ```
* **ข้อดี:** เสถียรที่สุด, ข้อมูลอัปเดตแบบ Real-time, รองรับการขยายตัว (Scale) ในอนาคต

### Solution 2: สร้าง API ให้บอท (Bot as an API)
หากไม่ต้องการตั้ง Database ใหม่ และยังคงต้องการใช้ `levels.json` คุณสามารถทำให้ Bot ของคุณทำหน้าที่เป็นเซิร์ฟเวอร์ส่งข้อมูลไปในตัว

* **โครงสร้างการทำงาน:** ติดตั้ง `express.js` ไว้ในโปรเจกต์ของบอท เพื่อเปิด API Endpoints ให้ Website เรียกใช้
* **ตัวอย่าง Endpoints:**
  * `GET /api/users/:discordId` -> เว็บไซต์เรียกเพื่อขอดูยอด Level และ Token 
  * `POST /api/users/spend` -> เว็บไซต์เรียกเพื่อสั่งให้บอทหัก Token จากผู้ใช้เมื่อแลกของสำเร็จ
* **ข้อดี:** ไม่ต้องพึ่งพา Database ภายนอก 
* **ข้อเสีย:** หากบอทดับสนิท (Offline) เว็บไซต์จะไม่สามารถดึงข้อมูลได้เลย

### Solution 3: แชร์ไฟล์ `levels.json` ใน Server เดียวกัน (สำหรับโปรเจกต์ขนาดเล็ก)
* **โครงสร้างการทำงาน:** นำ Bot และ Website ไปรันไว้ในเครื่อง Server (VPS) ตัวเดียวกัน โดยให้ Website (Backend) เขียนโค้ดเพื่อเปิดอ่านและแก้ไขไฟล์ `levels.json` จากโฟลเดอร์ของบอทโดยตรง
* **ข้อเสีย:** มีความเสี่ยงที่ไฟล์จะเสียหาย (Corrupt) หากบอทกำลังให้ XP และเว็บกำลังหัก Token ในเสี้ยววินาทีเดียวกัน (Race Condition) จึงไม่แนะนำสำหรับระบบที่ซีเรียสเรื่องข้อมูลสูญหาย

---

## 📝 ขั้นตอนเตรียมความพร้อม (Next Steps)

หากต้องการเตรียมตัวเพื่อรองรับระบบเว็บไซต์ในอนาคต ควรเริ่มจาก:
1. **ออกแบบระบบ Token:** กำหนดโครงสร้างข้อมูลใหม่โดยเพิ่ม `tokens` เข้าไปคู่กับ `xp` และ `level` 
2. **ปรับปรุงโค้ดบอท:** ให้รองรับการให้ Token แก่ผู้ใช้ (เช่น คำสั่ง `/daily`, การอัพเลเวล)
3. **ศึกษา Discord OAuth2:** เพื่อเป็นระบบ Authentication หลักของเว็บไซต์
4. **พิจารณาย้ายไปใช้ MongoDB:** เพื่อเตรียมพร้อมก่อนที่จะเริ่มเขียนฝั่งเว็บไซต์
